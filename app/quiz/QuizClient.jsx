"use client";
import {useEffect,useMemo,useState} from "react";
import {papers,questionsForMode} from "../../data/papers";
import {getOfficialMarking} from "../../data/markingData";

const KEY="ict-day-papers-quiz-v4";
const label=m=>m==="all"?"සියලුම පේපර්":`PHY ${String(Number(m.replace("paper-",""))).padStart(2,"0")}`;
const official=q=>getOfficialMarking(q.paperNumber,q.number).answer??q.answer;
function load(){try{return JSON.parse(localStorage.getItem(KEY)||'{"sessions":{}}')}catch{return {sessions:{}}}}

export default function QuizClient(){
 const [screen,setScreen]=useState("home"),[mode,setMode]=useState("all"),[answers,setAnswers]=useState({}),[index,setIndex]=useState(0),[marks,setMarks]=useState([]),[saved,setSaved]=useState({sessions:{}}),[ready,setReady]=useState(false),[zoom,setZoom]=useState(false);
 useEffect(()=>{setSaved(load());setReady(true)},[]);
 const questions=useMemo(()=>questionsForMode(mode),[mode]),q=questions[index],picked=q?answers[q.id]:undefined,marking=q?getOfficialMarking(q.paperNumber,q.number):null,correct=q?(marking?.answer??q.answer):undefined;
 const persist=(a=answers,i=index,b=marks,done=false)=>{if(!ready)return;setSaved(prev=>{const n={sessions:{...(prev.sessions||{}),[mode]:{answers:a,current:i,bookmarks:b,finished:done}},lastMode:mode};localStorage.setItem(KEY,JSON.stringify(n));return n})};
 const start=(m,restart=false)=>{const list=questionsForMode(m),s=restart?null:saved.sessions?.[m];setMode(m);setAnswers(s?.answers||{});setMarks(s?.bookmarks||[]);setIndex(Math.min(s?.current||0,Math.max(list.length-1,0)));setScreen("quiz");scrollTo(0,0)};
 const choose=c=>{if(!q||picked!==undefined)return;const n={...answers,[q.id]:c};setAnswers(n);persist(n,index,marks)};
 const go=i=>{const n=Math.max(0,Math.min(i,questions.length-1));setIndex(n);persist(answers,n,marks);scrollTo(0,0)};
 const toggle=()=>{if(!q)return;const n=marks.includes(q.id)?marks.filter(x=>x!==q.id):[...marks,q.id];setMarks(n);persist(answers,index,n)};
 const home=()=>{persist();setScreen("home");scrollTo(0,0)};
 const finish=()=>{persist(answers,index,marks,true);setScreen("results");scrollTo(0,0)};
 const stats=useMemo(()=>{let a=0,c=0;for(const x of questions){if(answers[x.id]!==undefined)a++;if(answers[x.id]===official(x))c++}return {a,c,w:a-c,u:questions.length-a}},[answers,questions]);

 if(screen==="home"){
  const total=papers.reduce((s,p)=>s+p.questions.length,0),lm=saved.lastMode,ls=lm?saved.sessions?.[lm]:null,lq=lm?questionsForMode(lm):[],la=ls?lq.filter(x=>ls.answers?.[x.id]!==undefined).length:0;
  return <main className="app-shell"><div className="ambient ambient-one"/><div className="ambient ambient-two"/><header className="site-header"><a className="brand" href="#top"><span className="brand-mark">IT</span><span><strong>ICT Day Papers</strong><small>2028 Quiz Studio</small></span></a><div className="source-pill">PHY 01–21 · මුල් පේපර් crop</div></header><section className="hero" id="top"><div className="hero-copy"><p className="eyebrow">Information &amp; Communication Technology</p><h1>දවසේ පේපර් සියල්ල<span> විභාග පුහුණුවක් කරගන්න.</span></h1><p className="hero-lead">ප්‍රශ්න පත්‍ර 21ක ප්‍රශ්න {total}ම මුල් අනුපිළිවෙළට. පේපර් එකෙන්ම ගත් crop image එක බලලා 1–5 පිළිතුර තෝරන්න. විවරණය text ලෙස නොව official marking PDF එකෙන්ම බලන්න.</p><div className="hero-actions"><button className="primary-button" onClick={()=>start("all")}>ප්‍රශ්න {total}ම පටන් ගන්න →</button>{lm&&ls&&la>0&&<button className="secondary-button" onClick={()=>start(lm)}>දිගටම {label(lm)} <span>{la}/{lq.length}</span></button>}</div><div className="hero-stats"><div><strong>{total}</strong><span>ප්‍රශ්න</span></div><div><strong>21</strong><span>පේපර්</span></div><div><strong>5</strong><span>පිළිතුරු</span></div></div></div><div className="hero-visual" aria-hidden="true"><div className="orbit orbit-one"/><div className="orbit orbit-two"/><div className="hero-card"><div className="hero-card-top"><span>LIVE PRACTICE</span><span>PHY 21</span></div><div className="mini-progress"><span style={{width:"68%"}}/></div><p>මුල් පේපර් crop</p><h2>පිළිතුර තෝරන්න</h2><div className="answer-preview">{[1,2,3,4,5].map(n=><span className={n===2?"active":""} key={n}>{n}</span>)}</div><div className="hero-card-result"><span>✓</span><div><strong>Official marking</strong><small>PDF එකෙන්ම විවරණය</small></div></div></div></div></section><section className="paper-section"><div className="section-heading"><div><p className="eyebrow">පේපර් එක තෝරන්න</p><h2>එක පේපර් එක බැගින් practice කරන්න</h2></div><p>Score, progress සහ save කළ ප්‍රශ්න මේ device එකේම තබා ගනී.</p></div><div className="paper-grid">{papers.map(p=>{const s=saved.sessions?.[`paper-${p.number}`],a=s?p.questions.filter(x=>s.answers?.[x.id]!==undefined).length:0,pc=Math.round(a/p.questions.length*100);return <button className="paper-card" key={p.id} onClick={()=>start(`paper-${p.number}`)}><div className="paper-card-top"><span className="paper-number">{String(p.number).padStart(2,"0")}</span><span className="question-count">{p.questions.length} ප්‍රශ්න</span></div><h3>{p.title}</h3><div className="paper-progress"><span style={{width:`${pc}%`}}/></div><div className="paper-card-footer"><span>{a?`${a}/${p.questions.length} කරලා`:"තව පටන්ගෙන නැහැ"}</span><span>↗</span></div></button>})}</div></section><footer className="site-footer"><p>Source papers & markings: Ravindu Bandaranayake · #ictfromabc.</p><p>Progress මේ device එකේම save වේ.</p></footer></main>
 }

 if(screen==="results"){
  const pc=questions.length?Math.round(stats.c/questions.length*100):0,first=questions.findIndex(x=>answers[x.id]!==official(x));
  return <main className="results-shell"><div className="results-card"><button className="back-link" onClick={home}>← පේපර් ලැයිස්තුවට</button><p className="eyebrow">Session complete</p><div className="score-ring" style={{"--score":`${pc*3.6}deg`}}><div><strong>{pc}%</strong><span>නිවැරදි ප්‍රතිශතය</span></div></div><h1>{pc>=80?"ඉතා හොඳයි!":pc>=60?"හොඳ ප්‍රගතියක්.":"තව practice කරමු."}</h1><p className="results-subtitle">{label(mode)} · {stats.c}/{questions.length} නිවැරදි</p><div className="results-grid"><div className="result-stat correct"><span>නිවැරදි</span><strong>{stats.c}</strong></div><div className="result-stat wrong"><span>වැරදි</span><strong>{stats.w}</strong></div><div className="result-stat unanswered"><span>නොකළ</span><strong>{stats.u}</strong></div></div><div className="results-actions">{first>=0&&<button className="primary-button" onClick={()=>{setIndex(first);setScreen("quiz")}}>වැරදි නැවත බලන්න →</button>}<button className="secondary-button" onClick={()=>start(mode,true)}>නැවත පටන් ගන්න</button></div></div></main>
 }
 if(!q)return null;
 const progress=(index+1)/questions.length*100,isCorrect=picked===correct,bookmarked=marks.includes(q.id);
 return <main className="quiz-shell"><header className="quiz-header"><button className="icon-button" onClick={home}>←</button><div className="quiz-title"><span>{mode==="all"?"සියලුම පේපර්":"පේපර් මෝඩ්"}</span><strong>{q.paperTitle}</strong></div><div className="header-actions"><a className="source-link" href={q.sourceUrl} target="_blank" rel="noreferrer">මුල් PDF ↗</a><button className="icon-button">{index+1}/{questions.length}</button></div></header><div className="top-progress"><span style={{width:`${progress}%`}}/></div><div className="quiz-layout"><aside className="quiz-sidebar"><div className="sidebar-brand"><span className="brand-mark">IT</span><div><strong>ICT Quiz</strong><small>2028 Day Papers</small></div></div><div className="sidebar-summary"><p>දැනට</p><strong>{stats.a}<span> / {questions.length}</span></strong><small>ප්‍රශ්න කරලා</small></div><div className="sidebar-legend"><span><i className="answered-dot"/> පිළිතුරු දුන්</span><span><i className="bookmark-dot"/> Save කළ</span></div></aside><section className="question-stage"><div className="question-meta"><div><span className="question-kicker">{q.paperTitle} · ප්‍රශ්නය {q.number}</span><h1>නිවැරදි පිළිතුර තෝරන්න</h1></div><button className={`bookmark-button ${bookmarked?"bookmarked":""}`} onClick={toggle}>{bookmarked?"★ Saved":"☆ Save"}</button></div><button className="question-image-card" onClick={()=>setZoom(true)}><img src={q.imageUrl} alt={q.alt}/><span className="zoom-hint">විශාල කර බලන්න</span></button><div className="answer-panel"><div className="answer-heading"><span>ඔබේ පිළිතුර</span><small>1–5 තෝරන්න</small></div><div className="answer-grid">{[1,2,3,4,5].map(c=>{let cls="";if(picked!==undefined)cls=c===correct?"correct":c===picked?"incorrect":"dimmed";return <button className={`answer-button ${cls}`} disabled={picked!==undefined} onClick={()=>choose(c)} key={c}><span>{c}</span>{cls==="correct"&&<i>✓</i>}{cls==="incorrect"&&<i>×</i>}</button>})}</div>{picked!==undefined&&<><div className={`feedback ${isCorrect?"feedback-correct":"feedback-wrong"}`}><span>{isCorrect?"✓":"!"}</span><div><strong>{isCorrect?"නිවැරදියි!":"වැරදියි — marking එක බලන්න."}</strong><p>Official marking පිළිතුර <b>{correct}</b>.</p></div></div><section className="marking-review"><div className="marking-review-head"><div><span>OFFICIAL MARKING</span><h2>විවරණය official PDF එකෙන්ම</h2></div><a href={marking?.openUrl} target="_blank" rel="noreferrer">Marking PDF එක අරින්න ↗</a></div><p style={{marginTop:12,color:'#66708a',lineHeight:1.6}}>මෙතන English text explanation නැහැ. Drive එකේ official marking PDF එකම පහළින් embed කරලා තියෙනවා.</p><iframe className="marking-frame" src={marking?.previewUrl} title={`${q.paperTitle} question ${q.number} marking`} loading="lazy"/></section></>}</div><div className="question-footer"><button className="previous-button" disabled={index===0} onClick={()=>go(index-1)}>← පෙර</button><span>{index+1} / {questions.length}</span><button className="next-button" onClick={()=>index===questions.length-1?finish():go(index+1)}>{index===questions.length-1?"ප්‍රතිඵල බලන්න":picked===undefined?"Skip":"ඊළඟ"} →</button></div></section></div>{zoom&&<div className="zoom-backdrop" onClick={()=>setZoom(false)}><button className="zoom-close">×</button><img src={q.imageUrl} alt={q.alt} onClick={e=>e.stopPropagation()}/></div>}<style jsx>{`.marking-review{margin-top:18px;border:1px solid rgba(54,93,255,.2);border-radius:22px;padding:20px;background:rgba(247,249,255,.96)}.marking-review-head{display:flex;justify-content:space-between;gap:16px}.marking-review-head span{font-size:11px;font-weight:800;letter-spacing:.12em;color:#5870d8}.marking-review-head h2{margin:5px 0 0;font-size:20px;color:#142342}.marking-review-head a{font-size:13px;font-weight:700;color:#3156d9;text-decoration:none}.marking-frame{width:100%;min-height:760px;margin-top:14px;border:1px solid #dde4f4;border-radius:16px;background:#fff}@media(max-width:680px){.marking-review{padding:14px}.marking-review-head{flex-direction:column}.marking-frame{min-height:620px}}`}</style></main>
}

export default {"answer":2,"explanations":["MAR stores memory addresses (not data), PC stores memory addresses (not instructions), and MDR stores data or instructions (not memory addresses). ys .nvd jkafka o;a;$Wmfoia ñi u;l ,smsk fkdfõ'","MAR (Memory Address Register) holds the memory address of the location in main memory that needs to be accessed for reading or writing. PC (Program Counter) holds the memory address of the next instruction to be fetched and executed. MDR (Memory Data Register) holds the actual data or instruction being transferred to or from main memory. lrhs' Why are other options incorrect? / fjk;a úl,am jerÈ wehs? Option / úl,am 1) MAR stores memory addresses (not data), PC stores memory addresses (not instructions), and MDR stores data or instructions (not memory addresses). ys .nvd jkafka o;a;$Wmfoia ñi u;l ,smsk fkdfõ' Option / úl,am 3) PC stores instruction addresses (not current data values), MAR stores memory addresses (not instructions), and MDR stores data/instructions (not CPU clock values).","PC stores instruction addresses (not current data values), MAR stores memory addresses (not instructions), and MDR stores data/instructions (not CPU clock values).","MAR stores addresses while MDR stores both data and instructions. PC stores instruction addresses, not data values. MAR ys ,smsk .nvd jk w;r MDR ys o;a; yd Wmfoia folu .nvd úh yel' PC ys .nvd jkafka o;a; w.hka fkdj Wmfoia ,smsk fõ'","PC stores the address of the next instruction (not the previous instruction), MAR stores memory addresses (not input data only), and MDR stores data/instructions (not output addresses). fkdj u;l ,smsk .nvd fõ' MDR ys m%;sodk ,smsk fkdj o;a;$Wmfoia .nvd fõ'"]};
export default {"answer":4,"explanations":["Statement A: Correct. Before an arithmetic or logical operation is performed, the required operands are normally loaded into registers. The ALU receives these values, performs the operation and returns the result to a register. fohs' Statement B: Incorrect. The accumulator stores intermediate results produced by the ALU, but it is a register rather than a processing component of the ALU. Its function is temporary storage, while the ALU performs the actual calculation. Statement C: Correct. The ALU can produce conditions such as carry, zero, negative and overflow after an operation. These conditions are recorded as flags in the status or flag register so that the CPU can use them for later decisions. Statement D: Correct. Registers retain binary values temporarily, whereas the ALU mainly receives values, transforms them through arithmetic or logical operations and sends the result back to a register. frðia;rhlg heùuhs'","Statement A: Correct. Before an arithmetic or logical operation is performed, the required operands are normally loaded into registers. The ALU receives these values, performs the operation and returns the result to a register. fohs' Statement C: Correct. The ALU can produce conditions such as carry, zero, negative and overflow after an operation. These conditions are recorded as flags in the status or flag register so that the CPU can use them for later decisions. Statement D: Correct. Registers retain binary values temporarily, whereas the ALU mainly receives values, transforms them through arithmetic or logical operations and sends the result back to a register. frðia;rhlg heùuhs'","Statement A: Correct. Before an arithmetic or logical operation is performed, the required operands are normally loaded into registers. The ALU receives these values, performs the operation and returns the result to a register. fohs' Statement B: Incorrect. The accumulator stores intermediate results produced by the ALU, but it is a register rather than a processing component of the ALU. Its function is temporary storage, while the ALU performs the actual calculation. Statement C: Correct. The ALU can produce conditions such as carry, zero, negative and overflow after an operation. These conditions are recorded as flags in the status or flag register so that the CPU can use them for later decisions. Statement D: Correct. Registers retain binary values temporarily, whereas the ALU mainly receives values, transforms them through arithmetic or logical operations and sends the result back to a register. frðia;rhlg heùuhs'","Statement A: Correct. Before an arithmetic or logical operation is performed, the required operands are normally loaded into registers. The ALU receives these values, performs the operation and returns the result to a register. fohs' Statement C: Correct. The ALU can produce conditions such as carry, zero, negative and overflow after an operation. These conditions are recorded as flags in the status or flag register so that the CPU can use them for later decisions. Statement D: Correct. Registers retain binary values temporarily, whereas the ALU mainly receives values, transforms them through arithmetic or logical operations and sends the result back to a register. frðia;rhlg heùuhs'","Statement A: Correct. Before an arithmetic or logical operation is performed, the required operands are normally loaded into registers. The ALU receives these values, performs the operation and returns the result to a register. fohs' Statement B: Incorrect. The accumulator stores intermediate results produced by the ALU, but it is a register rather than a processing component of the ALU. Its function is temporary storage, while the ALU performs the actual calculation. Statement C: Correct. The ALU can produce conditions such as carry, zero, negative and overflow after an operation. These conditions are recorded as flags in the status or flag register so that the CPU can use them for later decisions. Statement D: Correct. Registers retain binary values temporarily, whereas the ALU mainly receives values, transforms them through arithmetic or logical operations and sends the result back to a register. frðia;rhlg heùuhs'"]};